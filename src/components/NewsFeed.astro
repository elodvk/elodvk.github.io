---
import { getCollection } from 'astro:content';

const posts = await getCollection('docs', ({ id }) => {
  return id.startsWith('blog/');
});

// Sort posts by date (latest first)
const sortedPosts = posts.sort((a, b) => {
  const dateA = new Date(a.data.date || a.id.split('/').pop().slice(0, 10));
  const dateB = new Date(b.data.date || b.id.split('/').pop().slice(0, 10));
  return dateB - dateA;
}).slice(0, 4);

const formatDate = (post) => {
  const dateStr = post.data.date || post.id.split('/').pop().slice(0, 10);
  const date = new Date(dateStr);
  return date.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' });
};
---

<div class="news-feed-window not-content">
  <div class="window-header">
    <div class="header-controls">
      <span class="control">_</span>
      <span class="control">□</span>
      <span class="control">×</span>
    </div>
  </div>
  
  <div class="window-body">
    <div class="terminal-prompt">
      <span class="user">[root@purplesec</span> <span class="path">~]#</span> <span class="command">tail -f /var/log/latest.intel</span>
    </div>
    
    <div class="news-items">
      {sortedPosts.map(post => (
        <a href={`/${post.id}`} class="news-item">
          <span class="date">[{formatDate(post)}]</span>
          <span class="title">{post.data.title}</span>
        </a>
      ))}
    </div>
    
    <div class="scroll-indicators">
      <div class="arrow up">▲</div>
      <div class="arrow down">▼</div>
    </div>
  </div>
</div>

<style>
  .news-feed-window {
    background: var(--ps-window-bg);
    border: 1px solid var(--ps-window-border);
    border-radius: 8px;
    height: 100%;
    display: flex;
    flex-direction: column;
    font-family: 'JetBrains Mono', monospace;
  }

  .window-header {
    background: transparent;
    padding: clamp(1rem, 3vw, 1.5rem) clamp(1rem, 3vw, 1.5rem) 0;
    display: flex;
    justify-content: flex-end;
    border-bottom: none;
  }

  .header-controls {
    display: flex;
    gap: 6px;
    color: var(--ps-accent);
  }

  .control {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 16px;
    height: 16px;
    border: 1px solid var(--ps-accent);
    font-size: 10px;
    font-family: monospace;
    opacity: 0.8;
  }

  .window-body {
    padding: clamp(1rem, 3vw, 1.5rem);
    position: relative;
    flex-grow: 1;
    display: flex;
    flex-direction: column;
  }

  .terminal-prompt {
    font-size: 0.85rem;
    margin-bottom: 1.5rem;
    border-bottom: 1px solid var(--ps-window-border);
    padding-bottom: 0.5rem;
  }

  .terminal-prompt .user { color: var(--ps-accent); }
  .terminal-prompt .path { color: var(--ps-text); }
  .terminal-prompt .command { color: var(--ps-text); }

  .news-items {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .news-item {
    text-decoration: none;
    display: flex;
    gap: 0.75rem;
    font-size: 0.9rem;
    line-height: 1.4;
    transition: transform 0.2s;
  }

  .news-item:hover {
    transform: translateX(5px);
  }

  .news-item .date {
    color: var(--ps-accent);
    flex-shrink: 0;
  }

  .news-item .title {
    color: var(--ps-text);
    opacity: 0.9;
  }

  .news-item:hover .title {
    opacity: 1;
    color: var(--sl-color-accent-high);
    text-shadow: 0 0 8px var(--ps-accent-dim);
  }

  .scroll-indicators {
    position: absolute;
    right: 1rem;
    top: 50%;
    transform: translateY(-50%);
    display: flex;
    flex-direction: column;
    gap: clamp(2rem, 15vh, 8rem);
    color: var(--ps-accent);
    font-size: 0.7rem;
    opacity: 0.5;
  }

  .arrow {
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 0.3; }
    50% { opacity: 1; }
  }
</style>
